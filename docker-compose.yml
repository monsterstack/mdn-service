version: '2'
services:
  web:
    build: .
    ports:
      - "9616:9616"
    volumes:
      - .:/code
    environment:
      - HOST_IP=${HOST_IP}
      - SERVICE_NAME=MdnService
      - STAGE=${STAGE}
    extra_hosts:
      - "cdsp.digitalfunk.io:${HOST_IP}"
